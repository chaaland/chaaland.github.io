---
title: "Marching Squares"
categories:
  - Algorithms
date: 2026-05-01 19:00:00 +0000
mathjax: true
tags:
  - Algorithms
  - Computational Geometry
  - Visualization
toc: true
classes: wide
excerpt: "You have an equation f(x, y) = 0 but no way to draw it. Marching squares fixes that with a surprisingly simple idea."
---

## Level Sets

A level set of a 3D surface is given by the set of points

$$
\{(x,y)\quad |\quad f(x,y) = c\}.
$$

Informally, this corresponds to slicing the surface with a horizontal plane at height $$c$$ and graphing the curve that the intersection traces out onto the 2D plane.
They are essentially a 2D projection of 3D surface where the height information is encoded in the curve itself (i.e. distinct curves have distinct heights).

Figure 1 shows the level sets of the function $$f(x,y) = \sin(\pi x)\sin(\pi y)$$.

<style>
#ls-widget { max-width: 100%; }
</style>

<div class="widget-container" id="ls-widget">
  <svg class="widget-plot" id="ls-svg" viewBox="0 0 500 300" preserveAspectRatio="xMidYMid meet"></svg>
  <figcaption style="font-size: 0.9rem; color: #8b949e; margin-top: 12px;">Figure 1: Level sets of f(x,y) = sin(πx)sin(πy) for several values of c, colored by level value. Each curve is the set of points where the function equals the labeled constant.</figcaption>
</div>

This is what is generated by `matplotlib`'s `plt.contour` function.
But you might think that plotting the level set requires solving non-linear equations like 

$$\sin(\pi x)\sin(\pi y)=1/2.$$

It turns out there is a simple and elegant algorithm for drawing level sets without explicitly solving these equations called _marching squares_.

## Paraboloid Level Sets

Consider the function $$f(x,y) = x^2/4 + y^2$$.
The level sets take the form

$$
\{(x,y) | x^2/4 + y^2=c\}
$$

which are horizontal ellipses with the major axis twice as long as the minor axis as shown in Figure 2.

<style>
#pe-widget { max-width: 100%; }
</style>

<div class="widget-container" id="pe-widget">
  <svg class="widget-plot" id="pe-svg" viewBox="0 0 500 300" preserveAspectRatio="xMidYMid meet"></svg>
  <figcaption style="font-size: 0.9rem; color: #8b949e; margin-top: 12px;">Figure 2: Level sets of f(x,y) = x²/4 + y² for several values of c.</figcaption>
</div>

We can see that the level sets are ellipses by dividing through by $$c$$ to get

$$
\left(\frac{x}{2\sqrt{c}}\right)^2+\left(\frac{y}{\sqrt{c}}\right)^2=1.
$$

This is an ellipse with semi-major axis length $$2\sqrt{c}$$ and semi-minor axis length $$\sqrt{c}$$.[^fn1]

For a given level set $$c$$, the marching squares algorithm begins by evaluating $$f(x,y)$$ on a grid and determining whether each point is above or below $$c$$.[^fn2]
Figure 3 shows a 4 by 4 grid for the level set $$x^2/4+y^2=1$$.

<style>
#gc-widget { max-width: 100%; }
</style>

<div class="widget-container" id="gc-widget">
  <svg class="widget-plot" id="gc-svg" viewBox="0 0 500 300" preserveAspectRatio="xMidYMid meet"></svg>
  <figcaption style="font-size: 0.9rem; color: #8b949e; margin-top: 12px;">Figure 3: A 4×4 grid covering the domain. Blue corners lie inside the c = 1 ellipse (f(x,y) &lt; 1); grey corners lie outside. The dashed curve is the exact level set.</figcaption>
</div>

The python code below shows how to implement this subroutine.

{% highlight python %}
import numpy as np

def f(x: float, y: float) -> float:
    return x**2 / 4 + y**2 - 1

def label_corners(xs: np.ndarray, ys: np.ndarray) -> np.ndarray:
    """Return a boolean grid where True means the corner is inside (f < 0)."""
    return np.array([[f(x, y) < 0 for x in xs] for y in ys])

xs = np.linspace(-3, 3, 5)   # 5 points → 4×4 grid of cells
ys = np.linspace(-2, 2, 5)

inside = label_corners(xs, ys)
{% endhighlight %}

The next part of the algorithm is to examine each cell and determine if there are any edges where the level set crosses.
This occurs precisely when $$f(x,y)$$ goes from being above $$c$$ to below $$c$$ along an edge of the cell (i.e. when an edge connects a blue and gray circle).
To determine _where_ the level set crosses, we can use a simple linear interpolation.
Supposing $$f(x_a, y_a) > c$$ and $$f(x_b, y_b) \le c$$,

$$
\begin{align*}
x_{cross} &= \frac{c-f_b}{f_a - f_b} x_a + \left(1-\frac{c - f_b}{f_a - f_b}\right) x_b\\
y_{cross} &= \frac{c-f_b}{f_a - f_b} y_a + \left(1-\frac{c - f_b}{f_a - f_b}\right) y_b
\end{align*}
$$

This is a weighted averaged of the $$x$$ and $$y$$-coordinates, weighted by how close $$c$$ is to the corresponding values of $$f$$ at each corner.
We can easily enumerate the crossings for a given cell with the following python code

{% highlight python %}
from typing import TypeAlias

Point: TypeAlias = tuple[float, float]

def find_crossings(corners: list[Point], fvals: list[float]) -> list[Point]:
    """Return the edge crossing points for one cell of the marching squares grid."""
    crossings = []
    for k in range(4):
        nk = (k + 1) % 4
        fa, fb = fvals[k], fvals[nk]
        if (fa > 0) != (fb > 0):            # edge straddles the level set
            t = fa / (fa - fb)              # interpolation parameter ∈ (0, 1)
            xa, ya = corners[k]
            xb, yb = corners[nk]
            crossings.append((xa + t * (xb - xa), ya + t * (yb - ya)))
    return crossings
{% endhighlight %}

## Linear Interpolation on Crossing Edges

<style>
#sm-widget { max-width: 100%; }
.sm-panels { display: flex; gap: 16px; flex-wrap: wrap; justify-content: center; }
.sm-panel  { flex: 1; min-width: 280px; }
.sm-panel-title { text-align: center; font-size: 0.85rem; color: #8b949e; margin-top: 6px; font-style: italic; }
@media (max-width: 640px) { .sm-panels { flex-direction: column; align-items: center; } }
</style>

<div class="widget-container" id="sm-widget">
  <div class="sm-panels">
    <div class="sm-panel">
      <svg class="widget-plot" id="sm-left-svg" viewBox="0 0 300 260" preserveAspectRatio="xMidYMid meet"></svg>
      <div class="sm-panel-title">5×5 grid — click a cell</div>
    </div>
    <div class="sm-panel">
      <svg class="widget-plot" id="sm-right-svg" viewBox="0 0 300 260" preserveAspectRatio="xMidYMid meet"></svg>
      <div class="sm-panel-title">Cell detail</div>
    </div>
  </div>
  <figcaption style="font-size: 0.9rem; color: #8b949e; margin-top: 12px;">Figure 4: Click any cell in the left panel to inspect it. Blue dots are inside the ellipse (f &lt; 0), grey dots are outside. Diamonds mark where the contour crosses each edge; the t value gives the interpolation parameter along that edge.</figcaption>
</div>

## The Saddle Ambiguity

## Examples and Convergence

[^fn2]: Checking whether $$f(x,y) > c$$ or $$f(x,y) < c$$ is equivalent to checking the sign of $$g(x,y) = f(x,y) - c$$, so without loss of generality the algorithm tracks the zero level set of $$g$$.
[^fn1]: The $$c = 1$$ ellipse can be parameterized as $$x(\theta) = 2\cos(\theta)$$, $$y(\theta) = \sin(\theta)$$ for $$\theta \in [0, 2\pi)$$; more generally $$x(\theta) = 2\sqrt{c}\cos(\theta)$$, $$y(\theta) = \sqrt{c}\sin(\theta)$$. Most level sets do not admit such a closed form, which is why marching squares works from function evaluations alone.

{% include widget-scripts.html %}
<script>
// ── Figure 1: level sets of sin(πx)sin(πy) ───────────────────────────────────
(function () {
  'use strict';
  var NS = 'http://www.w3.org/2000/svg';

  function el(tag, attrs) {
    var e = document.createElementNS(NS, tag);
    for (var k in attrs) if (Object.prototype.hasOwnProperty.call(attrs, k)) e.setAttribute(k, attrs[k]);
    return e;
  }
  function txt(content, attrs) { var e = el('text', attrs); e.textContent = content; return e; }

  var svg = document.getElementById('ls-svg');
  if (!svg) return;

  // ── layout ───────────────────────────────────────────────────────────────────
  var W = 500, H = 300;
  var PD = { t: 25, r: 120, b: 40, l: 50 };
  var PW = W - PD.l - PD.r;   // 330
  var PH = H - PD.t - PD.b;   // 235
  var X0 = -2, X1 = 2, Y0 = -2, Y1 = 2;
  function px(x) { return PD.l + (x - X0) / (X1 - X0) * PW; }
  function py(y) { return PD.t + (Y1 - y) / (Y1 - Y0) * PH; }

  // ── math ─────────────────────────────────────────────────────────────────────
  function f(x, y) { return Math.sin(Math.PI * x) * Math.sin(Math.PI * y); }

  // ── palette: muted dusk (periwinkle → teal → sage → honey → terracotta) ──────
  var PAL = [[74,110,158],[74,140,168],[106,171,150],[196,160,85],[192,102,72]];
  function palette(t) {
    t = Math.max(0, Math.min(1, t));
    var s = t * 4, lo = Math.floor(s), hi = Math.min(lo + 1, 4), u = s - lo;
    return 'rgb(' +
      Math.round(PAL[lo][0] + u*(PAL[hi][0]-PAL[lo][0])) + ',' +
      Math.round(PAL[lo][1] + u*(PAL[hi][1]-PAL[lo][1])) + ',' +
      Math.round(PAL[lo][2] + u*(PAL[hi][2]-PAL[lo][2])) + ')';
  }

  // ── contour extraction (marching squares) ────────────────────────────────────
  var N = 80;
  var gdx = (X1 - X0) / N, gdy = (Y1 - Y0) / N;
  var vals = [];
  for (var j = 0; j <= N; j++) {
    vals[j] = [];
    for (var i = 0; i <= N; i++) vals[j][i] = f(X0 + i * gdx, Y0 + j * gdy);
  }

  function edgeCross(xa, ya, fa, xb, yb, fb) {
    if ((fa > 0) === (fb > 0)) return null;
    var t = fa / (fa - fb);
    return [xa + t * (xb - xa), ya + t * (yb - ya)];
  }

  function contourSegs(c) {
    var segs = [];
    for (var j = 0; j < N; j++) {
      for (var i = 0; i < N; i++) {
        var cx0 = X0 + i * gdx, cy0 = Y0 + j * gdy;
        var cx1 = cx0 + gdx,    cy1 = cy0 + gdy;
        var f00 = vals[j][i]   - c, f10 = vals[j][i+1]   - c;
        var f01 = vals[j+1][i] - c, f11 = vals[j+1][i+1] - c;
        var pts = [], p;
        if ((p = edgeCross(cx0, cy0, f00, cx1, cy0, f10))) pts.push(p);  // bottom
        if ((p = edgeCross(cx1, cy0, f10, cx1, cy1, f11))) pts.push(p);  // right
        if ((p = edgeCross(cx0, cy1, f01, cx1, cy1, f11))) pts.push(p);  // top
        if ((p = edgeCross(cx0, cy0, f00, cx0, cy1, f01))) pts.push(p);  // left
        if (pts.length === 2) {
          segs.push(pts);
        } else if (pts.length === 4) {
          // saddle: pair by lower-left corner sign
          if (f00 < 0) { segs.push([pts[0], pts[3]]); segs.push([pts[1], pts[2]]); }
          else         { segs.push([pts[0], pts[1]]); segs.push([pts[2], pts[3]]); }
        }
      }
    }
    return segs;
  }

  // ── draw ─────────────────────────────────────────────────────────────────────
  svg.appendChild(el('rect', { x: 0, y: 0, width: W, height: H, fill: '#0d1117' }));

  // grid
  for (var gx = X0; gx <= X1; gx++) {
    svg.appendChild(el('line', { x1: px(gx), y1: PD.t, x2: px(gx), y2: H - PD.b,
      stroke: '#21262d', 'stroke-width': 1 }));
  }
  for (var gy = Y0; gy <= Y1; gy++) {
    svg.appendChild(el('line', { x1: PD.l, y1: py(gy), x2: W - PD.r, y2: py(gy),
      stroke: '#21262d', 'stroke-width': 1 }));
  }

  // level curves
  var levels = [-0.75, -0.5, -0.25, 0, 0.25, 0.5, 0.75];
  levels.forEach(function (c) {
    var color = palette((c + 1) / 2);
    var isZero = (c === 0);
    contourSegs(c).forEach(function (seg) {
      svg.appendChild(el('line', {
        x1: px(seg[0][0]), y1: py(seg[0][1]),
        x2: px(seg[1][0]), y2: py(seg[1][1]),
        stroke: color, 'stroke-width': isZero ? 1.2 : 2, opacity: isZero ? 0.5 : 0.9
      }));
    });
  });

  // axes
  svg.appendChild(el('line', { x1: PD.l, y1: H-PD.b, x2: W-PD.r, y2: H-PD.b, stroke: '#8b949e', 'stroke-width': 1 }));
  svg.appendChild(el('line', { x1: PD.l, y1: PD.t, x2: PD.l, y2: H-PD.b, stroke: '#8b949e', 'stroke-width': 1 }));
  [X0, -1, 0, 1, X1].forEach(function (v) {
    var ppx = px(v);
    svg.appendChild(el('line', { x1: ppx, y1: H-PD.b, x2: ppx, y2: H-PD.b+4, stroke: '#8b949e', 'stroke-width': 1 }));
    svg.appendChild(txt(v, { x: ppx, y: H-PD.b+15, 'text-anchor': 'middle', fill: '#8b949e', 'font-size': 10, 'font-family': 'monospace' }));
  });
  [Y0, -1, 0, 1, Y1].forEach(function (v) {
    var ppy = py(v);
    svg.appendChild(el('line', { x1: PD.l-4, y1: ppy, x2: PD.l, y2: ppy, stroke: '#8b949e', 'stroke-width': 1 }));
    svg.appendChild(txt(v, { x: PD.l-7, y: ppy+4, 'text-anchor': 'end', fill: '#8b949e', 'font-size': 10, 'font-family': 'monospace' }));
  });
  svg.appendChild(txt('x', { x: PD.l+PW/2, y: H-5, 'text-anchor': 'middle', fill: '#8b949e', 'font-size': 11, 'font-family': 'monospace' }));
  var midY = PD.t + PH/2;
  svg.appendChild(txt('y', { x: 12, y: midY, 'text-anchor': 'middle', fill: '#8b949e', 'font-size': 11, 'font-family': 'monospace', transform: 'rotate(-90,12,' + midY + ')' }));

  // legend
  var lgX = W - PD.r + 18;
  svg.appendChild(txt('c', { x: lgX + 22, y: PD.t + 4, 'text-anchor': 'middle', fill: '#c9d1d9', 'font-size': 11, 'font-family': 'monospace', 'font-style': 'italic' }));
  levels.slice().reverse().forEach(function (c, idx) {
    var color = palette((c + 1) / 2);
    var ly = PD.t + 18 + idx * 28;
    svg.appendChild(el('line', { x1: lgX, y1: ly, x2: lgX+18, y2: ly, stroke: color, 'stroke-width': 2 }));
    svg.appendChild(txt((c >= 0 ? '+' : '') + c.toFixed(2), { x: lgX+24, y: ly+4, 'text-anchor': 'start', fill: '#c9d1d9', 'font-size': 10, 'font-family': 'monospace' }));
  });
})();

// ── Figure 2: level sets of x²/4 + y² (paraboloid ellipses) ─────────────────
(function () {
  'use strict';
  var NS = 'http://www.w3.org/2000/svg';

  function el(tag, attrs) {
    var e = document.createElementNS(NS, tag);
    for (var k in attrs) if (Object.prototype.hasOwnProperty.call(attrs, k)) e.setAttribute(k, attrs[k]);
    return e;
  }
  function txt(content, attrs) { var e = el('text', attrs); e.textContent = content; return e; }

  var svg = document.getElementById('pe-svg');
  if (!svg) return;

  // ── layout ───────────────────────────────────────────────────────────────────
  var W = 500, H = 300;
  var PD = { t: 25, r: 120, b: 40, l: 50 };
  var PW = W - PD.l - PD.r;
  var PH = H - PD.t - PD.b;
  var X0 = -3.6, X1 = 3.6, Y0 = -2, Y1 = 2;
  function px(x) { return PD.l + (x - X0) / (X1 - X0) * PW; }
  function py(y) { return PD.t + (Y1 - y) / (Y1 - Y0) * PH; }

  // ── palette: muted dusk (periwinkle → teal → sage → honey → terracotta) ──────
  var PAL = [[74,110,158],[74,140,168],[106,171,150],[196,160,85],[192,102,72]];
  function palette(t) {
    t = Math.max(0, Math.min(1, t));
    var s = t * 4, lo = Math.floor(s), hi = Math.min(lo + 1, 4), u = s - lo;
    return 'rgb(' +
      Math.round(PAL[lo][0] + u*(PAL[hi][0]-PAL[lo][0])) + ',' +
      Math.round(PAL[lo][1] + u*(PAL[hi][1]-PAL[lo][1])) + ',' +
      Math.round(PAL[lo][2] + u*(PAL[hi][2]-PAL[lo][2])) + ')';
  }

  // ── draw ─────────────────────────────────────────────────────────────────────
  svg.appendChild(el('rect', { x: 0, y: 0, width: W, height: H, fill: '#0d1117' }));

  // grid
  [-3, -1.5, 0, 1.5, 3].forEach(function (v) {
    svg.appendChild(el('line', { x1: px(v), y1: PD.t, x2: px(v), y2: H - PD.b,
      stroke: '#21262d', 'stroke-width': 1 }));
  });
  [-2, -1, 0, 1, 2].forEach(function (v) {
    svg.appendChild(el('line', { x1: PD.l, y1: py(v), x2: W - PD.r, y2: py(v),
      stroke: '#21262d', 'stroke-width': 1 }));
  });

  // level curves — ellipses drawn parametrically: x = 2√c·cos θ, y = √c·sin θ
  var levels = [0.25, 0.5, 1, 1.5, 2, 2.5];
  var cMin = levels[0], cMax = levels[levels.length - 1];
  levels.forEach(function (c) {
    var color = palette((c - cMin) / (cMax - cMin));
    var a = 2 * Math.sqrt(c), b = Math.sqrt(c);
    var pts = [];
    for (var k = 0; k <= 200; k++) {
      var angle = k / 200 * 2 * Math.PI;
      pts.push(px(a * Math.cos(angle)) + ',' + py(b * Math.sin(angle)));
    }
    svg.appendChild(el('path', { d: 'M ' + pts.join(' L ') + ' Z',
      fill: 'none', stroke: color, 'stroke-width': 2, opacity: 0.9 }));
  });

  // axes
  svg.appendChild(el('line', { x1: PD.l, y1: H-PD.b, x2: W-PD.r, y2: H-PD.b, stroke: '#8b949e', 'stroke-width': 1 }));
  svg.appendChild(el('line', { x1: PD.l, y1: PD.t, x2: PD.l, y2: H-PD.b, stroke: '#8b949e', 'stroke-width': 1 }));
  [-3, 0, 3].forEach(function (v) {
    var ppx = px(v);
    svg.appendChild(el('line', { x1: ppx, y1: H-PD.b, x2: ppx, y2: H-PD.b+4, stroke: '#8b949e', 'stroke-width': 1 }));
    svg.appendChild(txt(v, { x: ppx, y: H-PD.b+15, 'text-anchor': 'middle', fill: '#8b949e', 'font-size': 10, 'font-family': 'monospace' }));
  });
  [-2, 0, 2].forEach(function (v) {
    var ppy = py(v);
    svg.appendChild(el('line', { x1: PD.l-4, y1: ppy, x2: PD.l, y2: ppy, stroke: '#8b949e', 'stroke-width': 1 }));
    svg.appendChild(txt(v, { x: PD.l-7, y: ppy+4, 'text-anchor': 'end', fill: '#8b949e', 'font-size': 10, 'font-family': 'monospace' }));
  });
  svg.appendChild(txt('x', { x: PD.l+PW/2, y: H-5, 'text-anchor': 'middle', fill: '#8b949e', 'font-size': 11, 'font-family': 'monospace' }));
  var midY = PD.t + PH/2;
  svg.appendChild(txt('y', { x: 12, y: midY, 'text-anchor': 'middle', fill: '#8b949e', 'font-size': 11, 'font-family': 'monospace', transform: 'rotate(-90,12,' + midY + ')' }));

  // legend
  var lgX = W - PD.r + 18;
  svg.appendChild(txt('c', { x: lgX + 22, y: PD.t + 4, 'text-anchor': 'middle', fill: '#c9d1d9', 'font-size': 11, 'font-family': 'monospace', 'font-style': 'italic' }));
  levels.slice().reverse().forEach(function (c, idx) {
    var color = palette((c - cMin) / (cMax - cMin));
    var ly = PD.t + 18 + idx * 30;
    svg.appendChild(el('line', { x1: lgX, y1: ly, x2: lgX+18, y2: ly, stroke: color, 'stroke-width': 2 }));
    svg.appendChild(txt(c.toFixed(2), { x: lgX+24, y: ly+4, 'text-anchor': 'start', fill: '#c9d1d9', 'font-size': 10, 'font-family': 'monospace' }));
  });
})();

// ── Figure 3: grid corner coloring for c = 1 ellipse ─────────────────────────
(function () {
  'use strict';
  var NS = 'http://www.w3.org/2000/svg';

  function el(tag, attrs) {
    var e = document.createElementNS(NS, tag);
    for (var k in attrs) if (Object.prototype.hasOwnProperty.call(attrs, k)) e.setAttribute(k, attrs[k]);
    return e;
  }
  function txt(content, attrs) { var e = el('text', attrs); e.textContent = content; return e; }

  var svg = document.getElementById('gc-svg');
  if (!svg) return;

  // ── layout ───────────────────────────────────────────────────────────────────
  var W = 500, H = 300;
  var PD = { t: 25, r: 20, b: 40, l: 50 };
  var PW = W - PD.l - PD.r;
  var PH = H - PD.t - PD.b;
  var X0 = -3.6, X1 = 3.6, Y0 = -2.5, Y1 = 2.5;
  function px(x) { return PD.l + (x - X0) / (X1 - X0) * PW; }
  function py(y) { return PD.t + (Y1 - y) / (Y1 - Y0) * PH; }

  // ── math ─────────────────────────────────────────────────────────────────────
  var XS = [-3, -1.5, 0, 1.5, 3];
  var YS = [-2, -1, 0, 1, 2];
  function f(x, y) { return x * x / 4 + y * y - 1; }

  // ── colors ───────────────────────────────────────────────────────────────────
  var C_INSIDE  = '#6aabcf';
  var C_OUTSIDE = '#8b949e';
  var C_GRID    = '#21262d';

  // ── draw ─────────────────────────────────────────────────────────────────────
  svg.appendChild(el('rect', { x: 0, y: 0, width: W, height: H, fill: '#0d1117' }));

  // grid lines
  XS.forEach(function (x) {
    svg.appendChild(el('line', { x1: px(x), y1: PD.t, x2: px(x), y2: H - PD.b,
      stroke: C_GRID, 'stroke-width': 1 }));
  });
  YS.forEach(function (y) {
    svg.appendChild(el('line', { x1: PD.l, y1: py(y), x2: W - PD.r, y2: py(y),
      stroke: C_GRID, 'stroke-width': 1 }));
  });

  // c=1 ellipse: x = 2cosθ, y = sinθ  (dashed)
  var epts = [];
  for (var k = 0; k <= 200; k++) {
    var angle = k / 200 * 2 * Math.PI;
    epts.push(px(2 * Math.cos(angle)) + ',' + py(Math.sin(angle)));
  }
  svg.appendChild(el('path', { d: 'M ' + epts.join(' L ') + ' Z',
    fill: 'none', stroke: C_INSIDE, 'stroke-width': 2,
    'stroke-dasharray': '6,4', opacity: 0.7 }));

  // corner dots
  XS.forEach(function (x) {
    YS.forEach(function (y) {
      svg.appendChild(el('circle', {
        cx: px(x), cy: py(y), r: 3.5,
        fill: f(x, y) < 0 ? C_INSIDE : C_OUTSIDE
      }));
    });
  });

  // axes
  svg.appendChild(el('line', { x1: PD.l, y1: H-PD.b, x2: W-PD.r, y2: H-PD.b, stroke: C_OUTSIDE, 'stroke-width': 1 }));
  svg.appendChild(el('line', { x1: PD.l, y1: PD.t, x2: PD.l, y2: H-PD.b, stroke: C_OUTSIDE, 'stroke-width': 1 }));
  [-3, 0, 3].forEach(function (v) {
    var ppx = px(v);
    svg.appendChild(el('line', { x1: ppx, y1: H-PD.b, x2: ppx, y2: H-PD.b+4, stroke: C_OUTSIDE, 'stroke-width': 1 }));
    svg.appendChild(txt(v, { x: ppx, y: H-PD.b+15, 'text-anchor': 'middle', fill: C_OUTSIDE, 'font-size': 10, 'font-family': 'monospace' }));
  });
  [-2, 0, 2].forEach(function (v) {
    var ppy = py(v);
    svg.appendChild(el('line', { x1: PD.l-4, y1: ppy, x2: PD.l, y2: ppy, stroke: C_OUTSIDE, 'stroke-width': 1 }));
    svg.appendChild(txt(v, { x: PD.l-7, y: ppy+4, 'text-anchor': 'end', fill: C_OUTSIDE, 'font-size': 10, 'font-family': 'monospace' }));
  });
  svg.appendChild(txt('x', { x: PD.l+PW/2, y: H-5, 'text-anchor': 'middle', fill: C_OUTSIDE, 'font-size': 11, 'font-family': 'monospace' }));
  var midY = PD.t + PH/2;
  svg.appendChild(txt('y', { x: 12, y: midY, 'text-anchor': 'middle', fill: C_OUTSIDE, 'font-size': 11, 'font-family': 'monospace', transform: 'rotate(-90,12,' + midY + ')' }));
})();

// ── Figure 4: marching squares cell inspector ─────────────────────────────────
(function () {
  'use strict';
  var NS = 'http://www.w3.org/2000/svg';

  // ── helpers ──────────────────────────────────────────────────────────────
  function el(tag, attrs) {
    var e = document.createElementNS(NS, tag);
    for (var k in attrs) if (Object.prototype.hasOwnProperty.call(attrs, k)) e.setAttribute(k, attrs[k]);
    return e;
  }
  function txt(content, attrs) { var e = el('text', attrs); e.textContent = content; return e; }

  // ── math ─────────────────────────────────────────────────────────────────
  var XS = [-3, -1.5, 0, 1.5, 3];
  var YS = [-2, -1, 0, 1, 2];
  function f(x, y) { return x * x / 4 + y * y - 1; }

  function edgeCrossing(ax, ay, fa, bx, by, fb) {
    var t = fa / (fa - fb);
    return { x: ax + t * (bx - ax), y: ay + t * (by - ay), t: t };
  }

  function cellCrossings(ci, cj) {
    var corners = [
      [XS[ci],     YS[cj]    ],   // k=0 lower-left
      [XS[ci + 1], YS[cj]    ],   // k=1 lower-right
      [XS[ci + 1], YS[cj + 1]],  // k=2 upper-right
      [XS[ci],     YS[cj + 1]]   // k=3 upper-left
    ];
    var fv = corners.map(function (c) { return f(c[0], c[1]); });
    var crossed = [];
    for (var k = 0; k < 4; k++) {
      var nk = (k + 1) % 4;
      if ((fv[k] > 0) !== (fv[nk] > 0)) {
        crossed.push(edgeCrossing(corners[k][0], corners[k][1], fv[k],
                                  corners[nk][0], corners[nk][1], fv[nk]));
      }
    }
    if (crossed.length === 2) return { segs: [[crossed[0], crossed[1]]], corners: corners, fv: fv, crossed: crossed };
    return { segs: [], corners: corners, fv: fv, crossed: crossed };
  }

  // ── colors ───────────────────────────────────────────────────────────────
  var C_BG       = '#0d1117';
  var C_INSIDE   = '#6aabcf';
  var C_OUTSIDE  = '#8b949e';
  var C_CONTOUR  = '#6aabcf';
  var C_REF      = '#3a4a56';
  var C_GRID     = '#21262d';
  var C_SEL      = '#6abf85';
  var C_TEXT     = '#c9d1d9';

  // ── left panel layout ────────────────────────────────────────────────────
  var LW = 300, LH = 260;
  var LPD = { t: 22, r: 15, b: 38, l: 42 };
  var LPW = LW - LPD.l - LPD.r;
  var LPH = LH - LPD.t - LPD.b;
  var LX0 = -3.6, LX1 = 3.6, LY0 = -2.5, LY1 = 2.5;
  function lx(x) { return LPD.l + (x - LX0) / (LX1 - LX0) * LPW; }
  function ly(y) { return LPD.t + (LY1 - y) / (LY1 - LY0) * LPH; }

  // ── right panel layout ───────────────────────────────────────────────────
  var RW = 300, RH = 260;
  var RPD = { t: 22, r: 20, b: 38, l: 42 };
  var RPW = RW - RPD.l - RPD.r;
  var RPH = RH - RPD.t - RPD.b;
  var rX0, rX1, rY0, rY1;
  function rx(x) { return RPD.l + (x - rX0) / (rX1 - rX0) * RPW; }
  function ry(y) { return RPD.t + (rY1 - y) / (rY1 - rY0) * RPH; }

  // ── state ────────────────────────────────────────────────────────────────
  var selI = 1, selJ = 1;

  // ── DOM ──────────────────────────────────────────────────────────────────
  var svgL = document.getElementById('sm-left-svg');
  var svgR = document.getElementById('sm-right-svg');
  if (!svgL || !svgR) return;

  // ── draw left ────────────────────────────────────────────────────────────
  function drawLeft() {
    svgL.innerHTML = '';
    svgL.appendChild(el('rect', { x: 0, y: 0, width: LW, height: LH, fill: C_BG }));

    // grid lines
    XS.forEach(function (x) {
      svgL.appendChild(el('line', { x1: lx(x), y1: LPD.t, x2: lx(x), y2: LH - LPD.b,
        stroke: C_GRID, 'stroke-width': 1 }));
    });
    YS.forEach(function (y) {
      svgL.appendChild(el('line', { x1: LPD.l, y1: ly(y), x2: LW - LPD.r, y2: ly(y),
        stroke: C_GRID, 'stroke-width': 1 }));
    });

    // clickable cell rects (drawn before contour so they don't cover dots)
    for (var ci = 0; ci < 4; ci++) {
      for (var cj = 0; cj < 4; cj++) {
        var isSel = (ci === selI && cj === selJ);
        var rx0 = lx(XS[ci]), ry1_ = ly(YS[cj]);
        var rx1_ = lx(XS[ci + 1]), ry0_ = ly(YS[cj + 1]);
        var cellRect = el('rect', {
          x: rx0, y: ry0_, width: rx1_ - rx0, height: ry1_ - ry0_,
          fill: isSel ? 'rgba(63,185,80,0.09)' : 'transparent',
          stroke: isSel ? C_SEL : 'transparent',
          'stroke-width': isSel ? 2 : 0,
          cursor: 'pointer'
        });
        (function (i, j, r) {
          r.addEventListener('click', function () { selI = i; selJ = j; drawLeft(); drawRight(); });
          r.addEventListener('mouseenter', function () {
            if (i !== selI || j !== selJ) r.setAttribute('fill', 'rgba(255,255,255,0.05)');
          });
          r.addEventListener('mouseleave', function () {
            if (i !== selI || j !== selJ) r.setAttribute('fill', 'transparent');
          });
        })(ci, cj, cellRect);
        svgL.appendChild(cellRect);
      }
    }

    // reference ellipse (dashed)
    var epts = [];
    for (var ti = 0; ti <= 200; ti++) {
      var a = ti / 200 * 2 * Math.PI;
      epts.push(lx(2 * Math.cos(a)) + ',' + ly(Math.sin(a)));
    }
    svgL.appendChild(el('path', { d: 'M ' + epts.join(' L '), fill: 'none',
      stroke: C_REF, 'stroke-width': 1.5, 'stroke-dasharray': '5,3', opacity: 0.8 }));

    // contour segments
    for (var si = 0; si < 4; si++) {
      for (var sj = 0; sj < 4; sj++) {
        var result = cellCrossings(si, sj);
        result.segs.forEach(function (seg) {
          svgL.appendChild(el('line', {
            x1: lx(seg[0].x), y1: ly(seg[0].y),
            x2: lx(seg[1].x), y2: ly(seg[1].y),
            stroke: C_CONTOUR, 'stroke-width': 2.5
          }));
        });
      }
    }

    // corner dots
    XS.forEach(function (x) {
      YS.forEach(function (y) {
        svgL.appendChild(el('circle', { cx: lx(x), cy: ly(y), r: 3,
          fill: f(x, y) < 0 ? C_INSIDE : C_OUTSIDE }));
      });
    });

    // axes
    svgL.appendChild(el('line', { x1: LPD.l, y1: LH - LPD.b, x2: LW - LPD.r, y2: LH - LPD.b,
      stroke: C_OUTSIDE, 'stroke-width': 1 }));
    svgL.appendChild(el('line', { x1: LPD.l, y1: LPD.t, x2: LPD.l, y2: LH - LPD.b,
      stroke: C_OUTSIDE, 'stroke-width': 1 }));
    [-3, 0, 3].forEach(function (v) {
      var px = lx(v);
      svgL.appendChild(el('line', { x1: px, y1: LH - LPD.b, x2: px, y2: LH - LPD.b + 4,
        stroke: C_OUTSIDE, 'stroke-width': 1 }));
      svgL.appendChild(txt(v, { x: px, y: LH - LPD.b + 15, 'text-anchor': 'middle',
        fill: C_OUTSIDE, 'font-size': 10, 'font-family': 'monospace' }));
    });
    [-2, 0, 2].forEach(function (v) {
      var py = ly(v);
      svgL.appendChild(el('line', { x1: LPD.l - 4, y1: py, x2: LPD.l, y2: py,
        stroke: C_OUTSIDE, 'stroke-width': 1 }));
      svgL.appendChild(txt(v, { x: LPD.l - 7, y: py + 4, 'text-anchor': 'end',
        fill: C_OUTSIDE, 'font-size': 10, 'font-family': 'monospace' }));
    });
    svgL.appendChild(txt('x', { x: LPD.l + LPW / 2, y: LH - 4, 'text-anchor': 'middle',
      fill: C_OUTSIDE, 'font-size': 11, 'font-family': 'monospace' }));
    var midY = LPD.t + LPH / 2;
    svgL.appendChild(txt('y', { x: 10, y: midY, 'text-anchor': 'middle',
      fill: C_OUTSIDE, 'font-size': 11, 'font-family': 'monospace',
      transform: 'rotate(-90,10,' + midY + ')' }));
  }

  // ── draw right ───────────────────────────────────────────────────────────
  function drawRight() {
    svgR.innerHTML = '';

    var ci = selI, cj = selJ;
    var x0 = XS[ci], x1 = XS[ci + 1], y0 = YS[cj], y1 = YS[cj + 1];
    var cellW = x1 - x0, cellH = y1 - y0;
    rX0 = x0 - 0.45 * cellW;  rX1 = x1 + 0.65 * cellW;
    rY0 = y0 - 0.45 * cellH;  rY1 = y1 + 0.65 * cellH;

    svgR.appendChild(el('rect', { x: 0, y: 0, width: RW, height: RH, fill: C_BG }));

    // clip path so reference ellipse doesn't overflow
    var clipId = 'sm-rclip';
    var defs = el('defs', {});
    var cp = el('clipPath', { id: clipId });
    cp.appendChild(el('rect', { x: RPD.l, y: RPD.t, width: RPW, height: RPH }));
    defs.appendChild(cp);
    svgR.appendChild(defs);

    // reference ellipse arc (clipped)
    var epts = [];
    for (var ti = 0; ti <= 300; ti++) {
      var a = ti / 300 * 2 * Math.PI;
      epts.push(rx(2 * Math.cos(a)) + ',' + ry(Math.sin(a)));
    }
    svgR.appendChild(el('path', { d: 'M ' + epts.join(' L '), fill: 'none',
      stroke: C_REF, 'stroke-width': 1.5, 'stroke-dasharray': '5,3', opacity: 0.8,
      'clip-path': 'url(#' + clipId + ')' }));

    // cell border
    svgR.appendChild(el('rect', {
      x: rx(x0), y: ry(y1), width: rx(x1) - rx(x0), height: ry(y0) - ry(y1),
      fill: 'none', stroke: '#444c56', 'stroke-width': 1.5
    }));

    // contour segment
    var result = cellCrossings(ci, cj);
    result.segs.forEach(function (seg) {
      svgR.appendChild(el('line', {
        x1: rx(seg[0].x), y1: ry(seg[0].y),
        x2: rx(seg[1].x), y2: ry(seg[1].y),
        stroke: C_CONTOUR, 'stroke-width': 2.5
      }));
    });

    // crossing diamonds + t labels
    var eps = 0.01;
    result.crossed.forEach(function (cr) {
      var px = rx(cr.x), py = ry(cr.y), d = 6;
      svgR.appendChild(el('polygon', {
        points: px + ',' + (py - d) + ' ' + (px + d) + ',' + py + ' ' +
                px + ',' + (py + d) + ' ' + (px - d) + ',' + py,
        fill: C_CONTOUR, stroke: C_BG, 'stroke-width': 1.2
      }));
      // figure out which edge and push label outward
      var offX, offY, anchor;
      if (Math.abs(cr.y - y0) < eps)       { offX =  0; offY =  17; anchor = 'middle'; }  // bottom
      else if (Math.abs(cr.x - x1) < eps)  { offX = 13; offY =   4; anchor = 'start';  }  // right
      else if (Math.abs(cr.y - y1) < eps)  { offX =  0; offY = -13; anchor = 'middle'; }  // top
      else                                  { offX =-13; offY =   4; anchor = 'end';    }  // left
      svgR.appendChild(txt('t=' + cr.t.toFixed(3), {
        x: px + offX, y: py + offY, 'text-anchor': anchor,
        fill: C_CONTOUR, 'font-size': 9, 'font-family': 'monospace'
      }));
    });

    // corner dots + f-value labels
    var cornerOffsets = [
      { dx: -15, dy: 17, anchor: 'end'   },  // k=0 lower-left
      { dx:  15, dy: 17, anchor: 'start' },  // k=1 lower-right
      { dx:  15, dy:-13, anchor: 'start' },  // k=2 upper-right
      { dx: -15, dy:-13, anchor: 'end'   }   // k=3 upper-left
    ];
    result.corners.forEach(function (c, k) {
      var fval = result.fv[k];
      var inside = fval < 0;
      var px = rx(c[0]), py = ry(c[1]);
      svgR.appendChild(el('circle', { cx: px, cy: py, r: 4,
        fill: inside ? C_INSIDE : C_OUTSIDE }));
      var off = cornerOffsets[k];
      svgR.appendChild(txt((fval >= 0 ? '+' : '') + fval.toFixed(2), {
        x: px + off.dx, y: py + off.dy, 'text-anchor': off.anchor,
        fill: inside ? C_INSIDE : C_OUTSIDE,
        'font-size': 9, 'font-family': 'monospace', 'font-weight': 'bold'
      }));
    });

    // axes
    svgR.appendChild(el('line', { x1: RPD.l, y1: RH - RPD.b, x2: RW - RPD.r, y2: RH - RPD.b,
      stroke: C_OUTSIDE, 'stroke-width': 1 }));
    svgR.appendChild(el('line', { x1: RPD.l, y1: RPD.t, x2: RPD.l, y2: RH - RPD.b,
      stroke: C_OUTSIDE, 'stroke-width': 1 }));
    [x0, x1].forEach(function (v) {
      var px = rx(v);
      svgR.appendChild(el('line', { x1: px, y1: RH - RPD.b, x2: px, y2: RH - RPD.b + 4,
        stroke: C_OUTSIDE, 'stroke-width': 1 }));
      svgR.appendChild(txt(v, { x: px, y: RH - RPD.b + 15, 'text-anchor': 'middle',
        fill: C_OUTSIDE, 'font-size': 10, 'font-family': 'monospace' }));
    });
    [y0, y1].forEach(function (v) {
      var py = ry(v);
      svgR.appendChild(el('line', { x1: RPD.l - 4, y1: py, x2: RPD.l, y2: py,
        stroke: C_OUTSIDE, 'stroke-width': 1 }));
      svgR.appendChild(txt(v, { x: RPD.l - 7, y: py + 4, 'text-anchor': 'end',
        fill: C_OUTSIDE, 'font-size': 10, 'font-family': 'monospace' }));
    });
    svgR.appendChild(txt('x', { x: RPD.l + RPW / 2, y: RH - 4, 'text-anchor': 'middle',
      fill: C_OUTSIDE, 'font-size': 11, 'font-family': 'monospace' }));
    var midY = RPD.t + RPH / 2;
    svgR.appendChild(txt('y', { x: 10, y: midY, 'text-anchor': 'middle',
      fill: C_OUTSIDE, 'font-size': 11, 'font-family': 'monospace',
      transform: 'rotate(-90,10,' + midY + ')' }));
  }

  drawLeft();
  drawRight();
})();
</script>
